//Style for MessageReceived

import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    main_container: {
        flex: 1,
        backgroundColor: 'white'
    },
    list_empty: {
        textAlign: 'center',
        color: 'grey',
        //fontWeight: 'bold',
        fontSize: 20,
        paddingTop: 80
    },
})

export default styles